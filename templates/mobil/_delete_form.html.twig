<form method="post" action="{{ path('app_mobil_delete', {'id': mobil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mobil.id) }}">
    <button class="btn">Delete</button>
</form>
